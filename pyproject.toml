[project]
name = "fys-tony-pi"
version = "0.1.0"
description = "TonyPi MCP server and client tooling"
requires-python = ">=3.11"
dependencies = [
  "mcp",
  "openai",
  "opencv-python>=4.13.0.92",
  "python-dotenv",
  "streamlit",
]
